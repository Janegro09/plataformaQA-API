/**
 * @fileoverview Modulo Programs | Schema para la matriz de programas y grupos
 * 
 * @version 1.0
 * 
 * @author Soluciones Digitales - Telecom Argentina S.A.
 * @author Ramiro Macciuci <rmacciucivicente@teco.com.ar>
 * @copyright Soluciones Digitales - Telecom Argentina
 * 
 * History:
 * 1.0 - Version principal
 */

// Incluimos controladores, modelos, schemas y modulos
const mongoose = require('mongoose');
const Schema   = mongoose.Schema;

var ProgramsByPerfilamientos = new Schema({
    programId: {
        type: String,
        required: true
    },
    PerfilamientoFileId: {
        type: String,
        required: true
    },
    createdAt: {
        type: Date,
        default: Date.now
    }
});

module.exports = mongoose.model('ProgramsByPerfilamientos',ProgramsByPerfilamientos);